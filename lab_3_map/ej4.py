# Lista original
numeros = [1, 2, 3, 4, 5]

# Usando map() con lambda para obtener el cuadrado de cada número
cuadrados = list(map(lambda x: x**2, numeros))

# Mostrar resultado
print("Cuadrados de los números:", cuadrados) 
