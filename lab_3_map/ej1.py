# Lista original
numeros = [1, 2, 3, 4, 5]

# Usando map() con lambda para multiplicar cada número por 10
multiplicados = list(map(lambda x: x * 10, numeros))

# Mostrar resultado
print("Lista multiplicada por 10:", multiplicados) 
